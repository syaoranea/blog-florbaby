import { DatacustomPipe } from './datacustom.pipe';

describe('DatacustomPipe', () => {
  it('create an instance', () => {
    const pipe = new DatacustomPipe();
    expect(pipe).toBeTruthy();
  });
});
